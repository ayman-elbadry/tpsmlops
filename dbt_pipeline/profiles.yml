# profiles.yml — Profil de connexion dbt pour DuckDB
# Pointe vers la base de données 'ventes.duckdb' située à la racine du projet.

dbt_pipeline:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: ../ventes.duckdb
